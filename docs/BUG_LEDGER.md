# Bug ledger

Evidence date: 2026-08-21. The final review bundle contains machine-readable test and browser evidence.

| Finding                                                                                            | Disposition                                                                                                                                                               | Evidence boundary                                   |
| -------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| Number keys `1`–`4` reported inactive                                                              | Not reproduced in clean local Chromium; the user identified a browser extension. Central command dispatch now proves one panel action per key and ignores editable focus. | Unit/component and standalone/Pages browser runs.   |
| Legacy login/token/task queue and inner Submit                                                     | Removed. Embedded paths expose only document/catalog ports; outer Label Studio owns lifecycle.                                                                            | Source scan and CE network log.                     |
| Sparse spectrogram could miss brief calls                                                          | Replaced with complete overlapping STFT window traversal and deterministic brief-call fixtures.                                                                           | Audio unit/property results.                        |
| Browser resampling and stereo ambiguity                                                            | Source WAV metadata is authoritative where available; stereo playback remains intact and Average/Max/Left/Right affect analysis only.                                     | Audio fixtures and browser UI checks.               |
| Resize inversion, display mutation, overlap reachability, playback-rate and selection-play defects | Repaired in reducer/pointer/player code with canonical-invariant tests and real pointer flows.                                                                            | Unit/component/Playwright results.                  |
| Blank, no-calls, and last-box deletion conflated                                                   | Repaired as three explicit scientific states.                                                                                                                             | Reducer, protocol, local/CE/Enterprise workflows.   |
| Local JSON/CSV and dirty-state claims unproved                                                     | Production Pages artifact now exercises open, annotate, download, resume, mismatch, no-calls, replace warning, refresh, and network denial.                               | Pages run evidence.                                 |
| CE assets staged before Nx overwrite                                                               | Assets are staged after the upstream production build under `/react-app/froglabel/`.                                                                                      | Installer tests, manifest, served network log.      |
| CE detached MobX-State-Tree reads after Update                                                     | Upstream patch stops retaining/dereferencing dead annotations and guards post-await actions. Browser warnings/errors are fatal.                                           | Two fresh normal-HTTP runs and restricted WSGI run. |
| CE fresh Submit/Update/export/Task Summary incomplete                                              | Implemented in fresh fixture runner with immediate Submit, stable result ID, reload, Task Summary, no-calls, blank rejection, export parse, and ORM snapshot.             | Served CE evidence.                                 |
| Catalog writes could partially apply                                                               | Strict preflight plus `transaction.atomic`, locked project/descriptor reads, immutable IDs, case-insensitive current-code planning, revision discipline, and clone check. | Python tests and CE database workflows.             |
| CLI depended on repository cwd                                                                     | Hydra configs, schemas, CE overlay, and Enterprise bundle are package resources; installed-wheel test runs from `/tmp`.                                                   | Installed-package evidence.                         |
| Model/compiler path advertised before a real contract                                              | Removed from supported CLI, package inclusion, docs, and acceptance requirements; optional provenance round-trip remains.                                                 | CLI help/package inspection/unit fixture.           |
| Older CE releases advertised                                                                       | Exact CE 1.23.0 only is accepted and claimed.                                                                                                                             | Installer tests and docs scan.                      |
| Enterprise used external `src`                                                                     | Replaced by deterministic self-contained inline ReactCode XML sharing the same domain/workspace source.                                                                   | XML scans and exact-code harness.                   |

Unverified external boundary: no authorized licensed Enterprise website was supplied, so every live Enterprise Gate 0 row remains unverified. No public Pages deployment was performed.
